// HOW TO STYLE REACT COMPONENT WITH CSS
// -------------------------------------
// (not including external frameworks or preprocessors)

// 1. EXTERNAL = which is greate Global styles or small projects
// 2. MODULES  = which is preffered for individual components that have their own unique styles
// 3. INLINE   = which tends to be good for any small components minimals styling use what's best for your project or your own personal preference 

import Button from "./Button.jsx"

function App() {

  return (
    <Button />
  )
}

export default App
